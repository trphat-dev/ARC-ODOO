<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="custom_signup_inherit" inherit_id="auth_signup.signup">
        <xpath expr="." position="replace">
            <t t-call="web.layout">
                <t t-set="title">Đăng ký</t>
                <t t-set="head">
                    <!-- Google Fonts: Inter & Playfair Display -->
                    <link rel="preconnect" href="https://fonts.googleapis.com"/>
                    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous"/>
                    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&amp;family=Playfair+Display:wght@400;500;600;700&amp;display=swap" rel="stylesheet"/>

                    <!-- Bootstrap 5 CDN -->
                    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
                    <!-- FontAwesome CDN -->
                    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>

                    <!-- Odoo SCSS Assets -->
                    <t t-call-assets="web.assets_frontend" t-css="true" t-js="false"/>
                </t>

                <!-- Page Content -->
                <div class="auth-page">
                    <div class="signup-card auth-card-wide">
                        <div class="auth-row">
                            <!-- Left side - Illustration -->
                            <div class="auth-illustration">
                                <img src="/custom_auth/static/src/img/logo.png" alt="HDC Logo" class="hdc-logo"/>
                                <img src="/custom_auth/static/src/img/signup.png" alt="Signup Illustration" class="auth-img"/>
                            </div>

                            <!-- Right side - Form -->
                            <div class="auth-form-section">
                                <!-- Title -->
                                <div class="signup-logo">
                                    <h1 class="signup-title">Đăng ký tài khoản</h1>
                                    <p class="signup-subtitle">Tạo tài khoản để truy cập hệ thống</p>
                                </div>

                                <!-- Form -->
                                <form class="signup-form" id="signup-form" action="javascript:void(0);">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                    <div class="form-row">
                                        <div class="form-group">
                                            <label class="form-label" for="name">Họ và tên <span style="color:#ef4444">*</span>
                                            </label>
                                            <input type="text" class="form-control" id="name" name="name" placeholder="Nguyễn Văn A" required="required"/>
                                        </div>
                                        <div class="form-group">
                                            <label class="form-label" for="phone">Số điện thoại <span style="color:#ef4444">*</span>
                                            </label>
                                            <input type="tel" class="form-control" id="phone" name="phone" placeholder="0909123456" pattern="[0-9]{10}" maxlength="10" required="required"/>
                                        </div>
                                    </div>

                                    <div class="form-group">
                                        <label class="form-label" for="email">Email <span style="color:#ef4444">*</span>
                                        </label>
                                        <input type="email" class="form-control" id="email" name="login" placeholder="email@domain.com" required="required"/>
                                    </div>

                                    <div class="form-row">
                                        <div class="form-group">
                                            <label class="form-label" for="password">Mật khẩu <span style="color:#ef4444">*</span>
                                            </label>
                                            <div class="password-wrapper">
                                                <input type="password" class="form-control" id="password" name="password" placeholder="••••••••" required="required" minlength="8"/>
                                                <button type="button" class="password-toggle-btn" id="togglePassword1">
                                                    <i class="fas fa-eye-slash"></i>
                                                </button>
                                            </div>
                                            <div class="password-requirements">
                                                <div class="req-item" id="req-length">
                                                    <i class="fa fa-circle"></i>Tối thiểu 8 ký tự</div>
                                                <div class="req-item" id="req-uppercase">
                                                    <i class="fa fa-circle"></i>Có chữ in hoa</div>
                                                <div class="req-item" id="req-number">
                                                    <i class="fa fa-circle"></i>Có chữ số</div>
                                                <div class="req-item" id="req-special">
                                                    <i class="fa fa-circle"></i>Có ký tự đặc biệt</div>
                                            </div>
                                        </div>
                                        <div class="form-group">
                                            <label class="form-label" for="confirm_password">Xác nhận mật khẩu <span style="color:#ef4444">*</span>
                                            </label>
                                            <div class="password-wrapper">
                                                <input type="password" class="form-control" id="confirm_password" name="confirm_password" placeholder="••••••••" required="required"/>
                                                <button type="button" class="password-toggle-btn" id="togglePassword2">
                                                    <i class="fas fa-eye-slash"></i>
                                                </button>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="form-check mb-3" style="font-size:0.85rem;">
                                        <input type="checkbox" class="form-check-input" id="terms" name="terms" required="required"/>
                                        <label class="form-check-label" for="terms">Tôi đồng ý với <a href="#" style="color:var(--primary-end)">điều khoản sử dụng</a>
                                        </label>
                                    </div>

                                    <button type="submit" class="btn-signup" id="signup-submit">
                                        <i class="fas fa-user-plus me-2"></i>Đăng ký
                                    </button>
                                </form>

                                <div class="signup-footer">Đã có tài khoản? <a href="/web/login">Đăng nhập</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Toast -->
                <div class="toast-container position-fixed top-0 end-0 p-3" style="z-index:1100;">
                    <div id="signup-toast" class="toast align-items-center border-0" role="alert">
                        <div class="d-flex">
                            <div class="toast-body" id="toast-message"></div>
                            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
                        </div>
                    </div>
                </div>

                <!-- Scripts -->
                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
                <script><![CDATA[
                    document.addEventListener('DOMContentLoaded', function() {
                        // Password toggle
                        ['togglePassword1', 'togglePassword2'].forEach(function(id) {
                            var btn = document.getElementById(id);
                            if (btn) {
                                btn.addEventListener('click', function() {
                                    var input = this.previousElementSibling;
                                    var type = input.type === 'password' ? 'text' : 'password';
                                    input.type = type;
                                    this.querySelector('i').className = type === 'password' ? 'fas fa-eye-slash' : 'fas fa-eye';
                                });
                            }
                        });
                        
                        // Password validation
                        var pw = document.getElementById('password');
                        if (pw) {
                            pw.addEventListener('input', function() {
                                var v = this.value;
                                updateReq('req-length', v.length >= 8);
                                updateReq('req-uppercase', /[A-Z]/.test(v));
                                updateReq('req-number', /[0-9]/.test(v));
                                updateReq('req-special', /[!@#$%^&*(),.?":{}|<>_\-+=\[\]\\;'`~]/.test(v));
                            });
                        }
                        
                        function updateReq(id, valid) {
                            var el = document.getElementById(id);
                            if (el) {
                                var icon = el.querySelector('i');
                                if (valid) { el.classList.add('req-valid'); icon.className = 'fa fa-check'; }
                                else { el.classList.remove('req-valid'); icon.className = 'fa fa-circle'; }
                            }
                        }
                        
                        // Phone input filter
                        var phone = document.getElementById('phone');
                        if (phone) phone.addEventListener('input', function() { this.value = this.value.replace(/[^0-9]/g, ''); });
                        
                        // Form submit - Direct signup (no OTP)
                        var form = document.getElementById('signup-form');
                        if (form) {
                            form.addEventListener('submit', function(e) {
                                e.preventDefault();
                                var name = document.getElementById('name').value.trim();
                                var email = document.getElementById('email').value.trim();
                                var phoneVal = document.getElementById('phone').value.trim();
                                var password = document.getElementById('password').value;
                                var confirmPassword = document.getElementById('confirm_password').value;
                                var terms = document.getElementById('terms').checked;
                                
                                if (!name || !email || !phoneVal || !password || !confirmPassword || !terms) { showToast('Vui lòng điền đầy đủ thông tin', 'warning'); return; }
                                if (password !== confirmPassword) { showToast('Mật khẩu xác nhận không khớp', 'danger'); return; }
                                
                                var btn = document.getElementById('signup-submit');
                                btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Đang tạo tài khoản...';
                                btn.disabled = true;
                                
                                var formData = {name: name, email: email, phone: phoneVal, password: password, confirm_password: confirmPassword};
                                
                                fetch('/web/signup/direct', {
                                    method: 'POST',
                                    headers: {'Content-Type': 'application/json'},
                                    body: JSON.stringify({jsonrpc: '2.0', method: 'call', params: formData})
                                })
                                .then(function(r) { return r.json(); })
                                .then(function(result) {
                                    if (result.result && result.result.success) {
                                        // Show success
                                        form.innerHTML = '<div style="text-align:center;padding:40px 20px;">' +
                                            '<div style="font-size:50px;color:#10b981;margin-bottom:16px;">✓</div>' +
                                            '<h3 style="color:#10b981;margin-bottom:8px;">Đăng ký thành công!</h3>' +
                                            '<p style="color:#6b7280;">' + (result.result.message || 'Tài khoản đã được tạo.') + '</p>' +
                                            '<p style="color:#9ca3af;font-size:14px;">Đang chuyển hướng đến trang đăng nhập...</p>' +
                                            '</div>';
                                        setTimeout(function() { window.location.href = result.result.redirect_url || '/web/login'; }, 2000);
                                    } else {
                                        showToast(result.result ? result.result.message : 'Có lỗi xảy ra', 'danger');
                                        btn.innerHTML = '<i class="fas fa-user-plus me-2"></i>Đăng ký';
                                        btn.disabled = false;
                                    }
                                })
                                .catch(function() {
                                    showToast('Có lỗi xảy ra khi tạo tài khoản', 'danger');
                                    btn.innerHTML = '<i class="fas fa-user-plus me-2"></i>Đăng ký';
                                    btn.disabled = false;
                                });
                            });
                        }
                        
                        function showToast(message, type) {
                            var toastEl = document.getElementById('signup-toast');
                            var body = document.getElementById('toast-message');
                            body.textContent = message;
                            toastEl.classList.remove('bg-success', 'bg-danger', 'bg-warning');
                            toastEl.classList.add('bg-' + type, 'text-white');
                            var toast = new bootstrap.Toast(toastEl, {delay: 5000});
                            toast.show();
                        }
                    });
                ]]>                </script>
            </t>
        </xpath>
    </template>
</odoo>