<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="otp_popup_template" name="OTP Popup Template">
        <!-- OTP Popup Modal -->
        <div id="otp-popup" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden flex items-center justify-center">
            <div class="bg-white rounded-2xl border border-gray-200 max-w-md w-full mx-4">
                <!-- Header -->
                <div class="bg-gradient-to-r from-blue-600 to-blue-700 text-white p-6 rounded-t-2xl">
                    <div class="flex items-center justify-between">
                        <h3 class="text-xl font-bold">Xác thực OTP</h3>
                        <button id="close-otp-popup" class="text-white hover:text-gray-200 transition-colors">
                            <i class="fas fa-times text-lg"></i>
                        </button>
                    </div>
                    <p class="text-blue-100 mt-2">Nhập mã 6 số đã được gửi đến số điện thoại của bạn</p>
                </div>
                
                <!-- Content -->
                <div class="p-6">
                    <!-- Phone Display -->
                    <div class="mb-6">
                        <p class="text-sm text-gray-600 mb-1">Mã OTP đã được gửi đến:</p>
                        <p id="otp-phone" class="font-medium text-gray-900"></p>
                    </div>
                    
                    <!-- OTP Input -->
                    <div class="mb-6">
                        <label class="block text-sm font-medium text-gray-700 mb-3">Nhập mã OTP (Demo: nhập 6 số bất kỳ)</label>
                        <div class="flex gap-2 justify-center">
                            <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-lg font-bold border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200" data-index="0"/>
                            <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-lg font-bold border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200" data-index="1"/>
                            <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-lg font-bold border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200" data-index="2"/>
                            <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-lg font-bold border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200" data-index="3"/>
                            <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-lg font-bold border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200" data-index="4"/>
                            <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-lg font-bold border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-200" data-index="5"/>
                        </div>
                        <p id="otp-error" class="text-red-500 text-sm mt-2 hidden"></p>
                    </div>
                    
                    <!-- Timer -->
                    <div class="mb-6 text-center">
                        <p class="text-sm text-gray-600">
                            Mã OTP sẽ hết hạn sau <span id="otp-timer" class="font-medium text-blue-600">05:00</span>
                        </p>
                    </div>
                    
                    <!-- Buttons -->
                    <div class="flex gap-3">
                        <button id="resend-otp" class="flex-1 py-3 px-4 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed text-sm whitespace-nowrap">
                            <i class="fas fa-redo mr-2"></i>
                            Gửi lại
                        </button>
                        <button id="verify-otp" class="flex-1 py-3 px-4 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors disabled:opacity-50 disabled:cursor-not-allowed">
                            <i class="fas fa-check mr-2"></i>
                            Xác nhận
                        </button>
                    </div>
                    
                    <!-- Loading -->
                    <div id="otp-loading" class="hidden mt-4 text-center">
                        <div class="inline-flex items-center text-blue-600">
                            <i class="fas fa-spinner fa-spin mr-2"></i>
                            Đang xử lý...
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </template>
</odoo> 