<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="fund_management_control.fee_schedule_edit_form" name="Chỉnh sửa Biểu phí">
        <t t-call="web.layout">
            <t t-set="title">Chỉnh sửa Biểu phí</t>
            <t t-call-assets="web.assets_backend" t-js="true"/>

            <t t-set="head">
                <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
                <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>
                <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet"/>
            </t>

            <!-- Header Container for Management -->
            <div id="headermana-container"></div>

            <div class="fund-management-container">
                <div class="dashboard-shell">
                    <!-- Sidebar Widget -->
                    <div id="sidebarWidget"></div>

                    <!-- Main Content -->
                    <main class="dashboard-main">
                        <div class="form-main-container">
                            <div class="form-card">
                                <div class="card-header-gradient">
                                    <h2 class="h3"><i class="fas fa-edit me-2"></i>Chỉnh sửa Biểu phí</h2>
                                    <p>Cập nhật thông tin chi tiết cho biểu phí.</p>
                                </div>

                                <form action="/fee_schedule/update" method="POST" id="feeScheduleEditForm" novalidate="1">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <input type="hidden" name="fee_id" t-att-value="fee.id"/>
                                    
                                    <!-- Section 1: Basic Info -->
                                    <div class="form-section">
                                        <div class="section-header">
                                            <div class="section-icon"><i class="fas fa-info-circle fa-lg text-primary"></i></div>
                                            <div>
                                                <h5 class="mb-1">Thông tin cơ bản</h5>
                                                <small class="text-muted">Các thông tin định danh chính của biểu phí.</small>
                                            </div>
                                        </div>
                                        <div class="row g-3">
                                            <div class="col-md-6">
                                                <label for="fee_name" class="form-label fw-semibold">Tên loại phí <span class="text-danger">*</span></label>
                                                <input type="text" class="form-control" id="fee_name" name="fee_name" required="1" t-att-value="fee.fee_name"/>
                                            </div>
                                            <div class="col-md-6">
                                                <label for="fee_code" class="form-label fw-semibold">Mã phí VSD <span class="text-danger">*</span></label>
                                                <input type="text" class="form-control" id="fee_code" name="fee_code" required="1" t-att-value="fee.fee_code"/>
                                            </div>
                                            <div class="col-md-6">
                                                <label for="fee_type" class="form-label fw-semibold">Loại phí</label>
                                                <select class="form-select" id="fee_type" name="fee_type">
                                                    <t t-foreach="fee_types" t-as="ft">
                                                        <option t-att-value="ft[0]" t-esc="ft[1]" t-att-selected="fee.fee_type == ft[0]"/>
                                                    </t>
                                                </select>
                                            </div>
                                            <div class="col-md-6">
                                                <label for="scheme_id" class="form-label fw-semibold">Áp dụng cho chương trình</label>
                                                <select class="form-select" id="scheme_id" name="scheme_id">
                                                    <option value="">-- Áp dụng cho tất cả --</option>
                                                    <t t-foreach="schemes" t-as="s">
                                                        <option t-att-value="s.id" t-esc="s.name" t-att-selected="fee.scheme_id.id == s.id"/>
                                                    </t>
                                                </select>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Section 2: Conditions -->
                                    <div class="form-section">
                                        <div class="section-header">
                                            <div class="section-icon"><i class="fas fa-filter fa-lg text-info"></i></div>
                                            <div>
                                                <h5 class="mb-1">Điều kiện áp dụng</h5>
                                                <small class="text-muted">Thiết lập khoảng giá trị để tính phí.</small>
                                            </div>
                                        </div>
                                        <div class="row g-3 align-items-center">
                                            <div class="col-md-6 col-lg-3">
                                                <label class="form-label fw-semibold">Toán tử 1</label>
                                                <select class="form-select" name="operator_1">
                                                    <t t-foreach="operators" t-as="op">
                                                        <option t-att-value="op[0]" t-esc="op[1]" t-att-selected="fee.operator_1 == op[0]"/>
                                                    </t>
                                                </select>
                                            </div>
                                            <div class="col-md-6 col-lg-3">
                                                <label class="form-label fw-semibold">Giá trị ban đầu <span class="text-danger">*</span></label>
                                                <input type="number" class="form-control" name="initial_value" placeholder="0" required="1" t-att-value="fee.initial_value"/>
                                            </div>
                                            <div class="col-md-6 col-lg-3">
                                                <label class="form-label fw-semibold">Toán tử 2</label>
                                                <select class="form-select" name="operator_2">
                                                    <t t-foreach="operators" t-as="op">
                                                        <option t-att-value="op[0]" t-esc="op[1]" t-att-selected="fee.operator_2 == op[0]"/>
                                                    </t>
                                                </select>
                                            </div>
                                            <div class="col-md-6 col-lg-3">
                                                <label class="form-label fw-semibold">Giá trị kết thúc <span class="text-danger">*</span></label>
                                                <input type="text" class="form-control" name="end_value" placeholder="1000000 hoặc NAV" required="1" t-att-value="fee.end_value"/>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <!-- Section 3: Config & Status -->
                                    <div class="form-section">
                                        <div class="section-header">
                                            <div class="section-icon"><i class="fas fa-cogs fa-lg text-success"></i></div>
                                            <div>
                                                <h5 class="mb-1">Cấu hình &amp; Trạng thái</h5>
                                                <small class="text-muted">Thiết lập tỉ lệ phí và kích hoạt.</small>
                                            </div>
                                        </div>
                                        <div class="row g-3">
                                            <div class="col-md-6">
                                                <label for="fee_rate" class="form-label fw-semibold">Tỉ lệ phí <span class="text-danger">*</span></label>
                                                <div class="input-group">
                                                    <input type="number" step="0.01" class="form-control" id="fee_rate" name="fee_rate" placeholder="1.5" required="1" t-att-value="fee.fee_rate"/>
                                                    <span class="input-group-text">%</span>
                                                </div>
                                            </div>
                                            <div class="col-md-6">
                                                <label class="form-label fw-semibold d-block mb-2">Trạng thái</label>
                                                <div t-attf-class="toggle-switch-card #{'active' if fee.activate else ''}" onclick="toggleSwitchCard(this)">
                                                    <input type="checkbox" class="d-none" name="activate" value="on" t-att-checked="fee.activate"/>
                                                    <div t-attf-class="custom-switch mb-2 #{'active' if fee.activate else ''}"><div class="switch-handle"></div></div>
                                                    <h6 class="fw-bold mb-0">Kích hoạt biểu phí</h6>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Action Buttons -->
                                    <div class="form-section">
                                        <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center">
                                            <a href="/fee_schedule_list" class="btn btn-form-secondary d-flex align-items-center justify-content-center">
                                                <i class="fas fa-times me-2"></i>Hủy bỏ
                                            </a>
                                            <button type="submit" class="btn btn-form-primary d-flex align-items-center justify-content-center">
                                                <i class="fas fa-save me-2"></i>Cập nhật
                                            </button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </main>
                </div>
            </div>

            <script>
                <![CDATA[
                function toggleSwitchCard(cardElement) {
                    const hiddenCheckbox = cardElement.querySelector('input[type="checkbox"]');
                    const customSwitch = cardElement.querySelector('.custom-switch');
                    const isActive = cardElement.classList.toggle('active');
                    customSwitch.classList.toggle('active', isActive);
                    hiddenCheckbox.checked = isActive;
                }
                ]]>
            </script>
        </t>
    </template>
</odoo>
