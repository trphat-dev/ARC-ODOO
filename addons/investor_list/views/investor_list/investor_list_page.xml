<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="investor_list_page" name="Investor List Page">
        <t t-call="web.layout">
            <t t-set="title">Danh sách Nhà đầu tư</t>
            <t t-set="head">
                <!-- Bootstrap CSS -->
                <link
                    rel="stylesheet"
                    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
                    integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
                    crossorigin="anonymous"
                />
                <!-- Bootstrap Icons -->
                <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet"/>
                <!-- Font Awesome -->
                <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"/>
                <!-- Google Fonts - Inter -->
                <link rel="preconnect" href="https://fonts.googleapis.com"/>
                <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous"/>
                <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet"/>
            </t>

            <!-- Header Container for Management -->
            <div id="headermana-container"></div>


            <!-- Main Container -->
            <div class="bo">
                <div class="bo-container">
                    <!-- Loading Spinner -->
                    <div id="loading-spinner" class="bo-loading">
                        <div class="bo-loading__spinner"></div>
                        <div class="bo-loading__text">Đang tải dữ liệu...</div>
                    </div>
                    
                    <!-- Error Container (hidden by default) -->
                    <div id="error-container" class="bo-empty-state" style="display: none;">
                        <i class="fas fa-exclamation-triangle bo-empty-state__icon text-danger"></i>
                        <h3 class="bo-empty-state__title">Có lỗi xảy ra</h3>
                        <p class="bo-empty-state__desc" id="error-message">Không thể tải dữ liệu</p>
                        <button class="bo-btn bo-btn--primary" onclick="location.reload()">
                            <i class="fas fa-sync-alt"></i> Thử lại
                        </button>
                    </div>
                    
                    <!-- Investor List Widget Container -->
                    <div id="investor-list-widget" class="w-100" style="display: none;"></div>
                </div>
            </div>

            <!-- Pass data to JavaScript -->
            <script type="text/javascript">
                // Parse the JSON data from controller
                var dashboardData = <t t-raw="all_dashboard_data or '{}'"/>;
                window.allDashboardData = {
                    investors: dashboardData.investors || []
                };
                
                window.showError = function(message) {
                    const errorContainer = document.getElementById('error-container');
                    const errorMessage = document.getElementById('error-message');
                    const loadingSpinner = document.getElementById('loading-spinner');
                    const widgetContainer = document.getElementById('investor-list-widget');
                    
                    if (errorContainer &amp;&amp; errorMessage) {
                        errorMessage.textContent = message || 'Có lỗi xảy ra khi tải dữ liệu';
                        errorContainer.style.display = 'block';
                    }
                    
                    if (loadingSpinner) {
                        loadingSpinner.style.display = 'none';
                    }
                    
                    if (widgetContainer) {
                        widgetContainer.style.display = 'none';
                    }
                };
                
                window.hideSpinner = function() {
                    const loadingSpinner = document.getElementById('loading-spinner');
                    const errorContainer = document.getElementById('error-container');
                    const widgetContainer = document.getElementById('investor-list-widget');
                    
                    if (loadingSpinner) {
                        loadingSpinner.style.display = 'none';
                    }
                    
                    if (errorContainer) {
                        errorContainer.style.display = 'none';
                    }
                    
                    if (widgetContainer) {
                        widgetContainer.style.display = 'block';
                    }
                };
            </script>

            <t t-call-assets="web.assets_frontend" t-js="true"/>
        </t>
    </template>
</odoo>
