<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <template id="overview_fund_management_page" name="Overview Fund Management Page">
    <t t-call="web.layout">
      <t t-set="title">Tổng Quan Quản Lý Quỹ</t>

      <!-- Import CSS -->
      <t t-set="head">
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
        <!-- Bootstrap Icons -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet"/>
        <!-- Google Fonts - Inter -->
        <link rel="preconnect" href="https://fonts.googleapis.com"/>
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous"/>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet"/>
        <!-- Font Awesome -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"/>
        <!-- Premium styles are loaded from SCSS file via Odoo assets -->
      </t>

      <!-- Header Container -->
      <div id="header-container"></div>
      
      <!-- Chatbot AI Widget Container -->
      <div id="chatbotAiWidgetContainer"></div>
      
      <!-- Main Container -->
      <div class="fund-overview-container">
        <div class="container-fluid py-4">
          <!-- Loading Spinner -->
          <div id="loading-spinner" class="loading-container">
            <div class="loading-spinner">
              <div class="spinner-border" role="status">
                <span class="visually-hidden">Đang tải...</span>
              </div>
              <div class="loading-text">Đang tải dữ liệu...</div>
            </div>
          </div>
          
          <!-- Error Container (hidden by default) -->
          <div id="error-container" class="error-container" style="display: none;">
            <div class="error-icon">
              <i class="bi bi-exclamation-triangle"></i>
            </div>
            <h4 class="error-title">Có lỗi xảy ra</h4>
            <p class="error-message" id="error-message">Không thể tải dữ liệu</p>
            <button class="btn btn-primary mt-3" onclick="location.reload()">
              <i class="bi bi-arrow-clockwise me-2"></i>
              Thử lại
            </button>
          </div>
          
          <!-- Widget Container -->
          <div id="overview-fund-management-widget" class="w-100" style="display: none;"></div>
        </div>
      </div>
      
      <!-- Footer Container -->
      <div id="footer-container"></div>
      
      <!-- Pass data from controller to JavaScript -->
      <script type="text/javascript">
        window.allDashboardData = <t t-raw="all_dashboard_data"/>;
        
        // Hàm tiện ích để hiển thị lỗi
        window.showError = function(message) {
          const errorContainer = document.getElementById('error-container');
          const errorMessage = document.getElementById('error-message');
          const loadingSpinner = document.getElementById('loading-spinner');
          const widgetContainer = document.getElementById('overview-fund-management-widget');
          
          if (errorContainer &amp;&amp; errorMessage) {
            errorMessage.textContent = message || 'Có lỗi xảy ra khi tải dữ liệu';
            errorContainer.style.display = 'block';
          }
          
          if (loadingSpinner) {
            loadingSpinner.style.display = 'none';
          }
          
          if (widgetContainer) {
            widgetContainer.style.display = 'none';
          }
        };
        
        // Hàm tiện ích để ẩn spinner và hiển thị widget
        window.hideSpinner = function() {
          const loadingSpinner = document.getElementById('loading-spinner');
          const errorContainer = document.getElementById('error-container');
          const widgetContainer = document.getElementById('overview-fund-management-widget');
          
          if (loadingSpinner) {
            loadingSpinner.style.display = 'none';
          }
          
          if (errorContainer) {
            errorContainer.style.display = 'none';
          }
          
          if (widgetContainer) {
            widgetContainer.style.display = 'block';
          }
        };
      </script>

      <!-- Import JavaScript -->
      <t t-set="js">
        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Chart.js -->
        <script src="https://cdn.jsdelivr.net/npm/chart.js@3.7.0/dist/chart.min.js"></script>
      </t>

      <t t-call-assets="web.assets_frontend" t-js="true"/>
    </t>
  </template>
</odoo> 