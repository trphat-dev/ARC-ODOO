# =============================================================================
# ARC-ODOO — Python Requirements for Odoo 18
# =============================================================================
# Chỉ liệt kê package THỰC SỰ được import trong code.
# Odoo 18 image đã có sẵn: werkzeug, psycopg2, lxml, Jinja2, etc.
# =============================================================================
#pip install --ignore-installed typing_extensions
# -----------------------------------------------------------------------------
# CORE
# -----------------------------------------------------------------------------
requests>=2.31.0                # HTTP client (ssi_gateway, controllers)
Pillow>=10.0.1                  # Image processing (ekyc, pdf_utils)

# -----------------------------------------------------------------------------
# SSI FASTCONNECT API (stock_data, stock_trading)
# -----------------------------------------------------------------------------
ssi-fc-data>=1.0.0              # SSI Market Data SDK
ssi-fctrading>=1.0.0            # SSI Trading SDK

# -----------------------------------------------------------------------------
# DATA & ANALYSIS
# -----------------------------------------------------------------------------
numpy>=1.24.0                   # Numerical computing (ai_trading_assistant)
pandas>=2.0.0                   # Data manipulation (ai_trading_assistant, reports)

# -----------------------------------------------------------------------------
# AI TRADING (ai_trading_assistant)
# -----------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
finrl @ git+https://github.com/AI4Finance-Foundation/FinRL.git
stable-baselines3>=2.0.0        # DRL algorithms
gymnasium>=0.29.0               # RL environments
scikit-learn>=1.3.0             # ML utilities

# -----------------------------------------------------------------------------
# PDF & REPORTING (fund_management, report_list)
# -----------------------------------------------------------------------------
PyMuPDF>=1.23.8                 # PDF manipulation (import fitz)
openpyxl>=3.1.2                 # Excel export (report_list controllers)

# -----------------------------------------------------------------------------
# UTILITIES
# -----------------------------------------------------------------------------
python-dateutil>=2.8.2          # Date parsing (dateutil)
pytz>=2023.3                    # Timezone handling